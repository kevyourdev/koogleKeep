import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

// 使用ReactDOM的render方法將App綁到有id為"root"的DOM element中
ReactDOM.render(<App />, document.getElementById("root"));